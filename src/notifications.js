// Notifications disabled for Expo Go (not supported in SDK 53+)
// Re-enable when using a development build

export async function registerForPushNotifications() {
    console.log('Push notifications disabled in Expo Go');
    return null;
}
